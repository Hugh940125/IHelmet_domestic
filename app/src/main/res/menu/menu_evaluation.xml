<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item android:id="@+id/action_camera"
          android:title="Camera"
          android:icon="@mipmap/camera"
          app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_sound"
          android:title="Sound"
          android:icon="@drawable/btn_voice2x"
          app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_reports"
          android:title="Sound"
          android:icon="@mipmap/moree"
          app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_nosound"
          android:title="Sound"
          android:icon="@mipmap/btn_mute2x"
          app:showAsAction="ifRoom"/>
</menu>