<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="NumberProgressBar_Twinkle_Night">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>

        <item name="progress_max">100</item>
        <item name="progress_current">0</item>

        <!-- 背景进度条颜色 -->
        <item name="progress_unreached_color">#E6E6E6</item>
        <!-- 前景进度条颜色 -->
        <item name="progress_reached_color">#3BB3DC</item>
        <item name="progress_text_size">16sp</item>
        <item name="progress_text_color">#3BB3DC</item>

        <!-- 前景进度条高度 -->
        <item name="progress_reached_bar_height">7dp</item>
        <!-- 背景进度条高度 -->
        <item name="progress_unreached_bar_height">7dp</item>
    </style>

    <style name="CustomToolBar" parent="Theme.AppCompat.NoActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="actionMenuTextColor">@color/white</item>
    </style>

    <style name="dateStyle">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!--<style name="RadioButton">
        <item name="rbd_width">32dp</item>
        <item name="rbd_height">32dp</item>
        <item name="rbd_strokeSize">2dp</item>
        <item name="rbd_radius">10dp</item>
        <item name="rbd_innerRadius">5dp</item>
        <item name="rbd_animDuration">@android:integer/config_mediumAnimTime</item>
        <item name="android:textColor">@drawable/tab_text_color_selector</item>
    </style>-->

    <style name="MonthProgressBar_Twinkle_Night">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="progress_max">177</item>
        <item name="progress_current">0</item>

        <!-- 背景进度条颜色 -->
        <item name="progress_unreached_color">#E6E6E6</item>
        <!-- 前景进度条颜色 -->
        <item name="progress_reached_color">#3BB3DC</item>
        <!-- 前景进度条字体size和颜色 -->
        <item name="progress_text_size">20sp</item>
        <item name="progress_text_color">#3BB3DC</item>

        <!-- 前景进度条高度 -->
        <item name="progress_reached_bar_height">4dp</item>
        <!-- 背景进度条高度 -->
        <item name="progress_unreached_bar_height">4dp</item>

        <!-- 进度条总内边距高度 -->
        <item name="android:paddingTop">100dp</item>

    </style>

    <!-- 选择照片底部弹出层 -->
    <style name="PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
